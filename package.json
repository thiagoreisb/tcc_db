{
  "name": "db",
  "version": "1.0.0",
  "description": "Simple application to connect to db",
  "main": "index.js",
  "scripts": {
    "start": "DATABASE_URL=$(heroku config:get DATABASE_URL -a immense-retreat-94183) node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Thiago R Bassetto",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^7.0.0",
    "express": "^4.16.4",
    "pg": "^7.10.0"
  },
  "engines": {
    "node": "8.10.0"
  }
}
